# 验证记录

2026-09-20，在 Linux 本机完成以下验证。

- `go test -race ./...`：全部通过。包括本机模拟上海电信门户的完整认证链、Cookie 传递、分类频道读取、组播探测及取消、覆盖配置持久化、并发写入、令牌保护、API key 脱敏、播放列表和路由兼容。
- 实际调用本机 `ffmpeg` 生成 MPEG-TS 测试流，再由识别服务截取 JPEG 并发送到模拟 Chat Completions API，验证建议保存且不覆盖频道名称。
- `go vet ./...`、`node --check modules/panel/web/app.js`、`git diff --check`：通过。
- `go build -trimpath -o bin/iptv-spider .`：构建 Linux 本机可执行文件。

## js-reverse 浏览器验证

通过已有 Chrome for Testing 的 `http://127.0.0.1:9222` CDP 地址连接 js-reverse-mcp。本机启动脚本为 `~/workspace/reverse/js-reverse-mcp/run-chrome-for-mcp.sh`，现有浏览器无需重新安装。连接参数为 `--browserUrl http://127.0.0.1:9222`。

测试实例监听 `127.0.0.1:18888`，使用 `/tmp/iptv-panel-e2e` 中的独立设置与测试数据。

实际使用 js-reverse 的 `new_page`、`click_element`、`evaluate_script`、`list_network_requests`、`list_console_messages` 和 `take_screenshot` 验证：

1. 令牌登录后显示面板，默认 VLAN 为 85。
2. 保存转发服务和 FCC，相关管理请求返回 200。
3. 添加频道，编辑分组与 Logo，列表正确显示；刷新页面后保留。
4. 保存识别 API 设置后 key 输入框清空，显示“已保存 key”，重载后仍不回显。
5. M3U/JSON 导出包含正确的频道字段、转发路径和 FCC。
6. 填入单播地址作为扫描范围时显示明确错误，扫描状态保持待命。

浏览器未出现未捕获 JavaScript 异常。测试期间预期的 HTTP 错误包括未登录 401、故意填写不存在的测试 Logo 404、非法扫描范围 400。

## 未实网验证

VLAN 创建通过模拟 `ip` 命令验证，没有修改本机真实网络。未扫描真实组播范围，未调用收费视觉模型。MySQL/OSS 集成与 Docker 镜像需在配置完成的部署环境验证。

## 本机默认网络真实认证

按用户授权，使用 `~/Downloads/shdx.php` 中的机顶盒身份，通过新 `modules/spider` 客户端实际测试：

- 使用本机默认网络，不配置 VLAN、不绑定源 IP、不使用 HTTP 代理。
- `222.68.208.73:7001` TCP 可达。
- 完整门户认证成功，耗时约 279 ms。
- 分类频道列表获取成功，共 168 个频道，总耗时约 518 ms；抽样为 CCTV-1 至 CCTV-5。

这证明当前网络路径可以完成 IPTV 认证和频道列表获取，不代表组播播放已通过测试。机顶盒认证 IP 仍使用 PHP 配置值，与本机实际出口 IP 分开。测试账号未写入仓库，临时身份配置已在测试后删除。

## 移除 VLAN 配置

认证已改为只使用系统默认网络，移除面板 VLAN/网卡/源 IP 表单和网络操作 API，以及 VLAN DHCP 诊断脚本。默认组播转发地址为 `192.168.190.1:4022`。配置版本从 1 迁移到 2，旧网络字段移除，账号、频道与 API key 保留。上述 VLAN 测试记录为移除前的历史记录。

## 台标、频道编辑、单播与 HTTP 回看（后续更新）

- 接入用户指定台标源 `https://iptv.yang-1989.xyz/logo`，实际目录含 526 个条目；内置快照、启动刷新，按名称匹配，手动 URL 优先。
- js-reverse 在隔离面板实测 ID 修改保存、重复 ID 拒绝、分组已有选项及自定义保存、识别建议应用、台标图片预览。自定义分组已改为下拉列表中的“自定义…”选项，选择后原位置输入。
- 回归测试覆盖 ID 修改后重启/同步不重复、自动台标及手动优先、单播 M3U、HTTP/RTSP 回看时间参数和认证取址。
- 真实账号获取 168 个频道，其中 167 个具备有效 RTSP 时移地址；另一个地址为字符串 `null`，已按无时移处理。抽样验证 CCTV-1 的 RTSP 转发 `192.168.190.1:4022/rtsp/…` 返回 HTTP 200 与 MPEG-TS 数据。
- 官方 HTTP 直播字段为 `liveUrl`；真实测试主清单、子清单和媒体分片均返回 200，分片符合 MPEG-TS 同步字节。
- 官方 HTTP 回看取得 m3u8（HTTP 200），抽样读取媒体分片（HTTP 206）并确认 MPEG-TS 同步字节。未声称验证全部频道、完整七天窗口或长时间连续播放。

- 最终服务保持 204 个频道（含用户扫描/自定义频道），同步未丢失这些配置。通过 `/api/play` 实测 HTTP 直播/回看及 RTSP 转 HTTP 直播/回看四种请求均 302 跳转，上游 HTTP 200，返回有效 HLS 或 TS 数据。最终导出 HTTP 168 条、RTSP 单播 167 条，167 条附带回看信息。
- js-reverse 登录最终面板，确认真实台标图片加载、ID 可编辑、分组“自定义…”与已有分组选择正常。

## 模型智能匹配台标

- 复用已配置的 OpenAI 兼容 API、key、模型，通过文本输入频道名称、分组及台标目录。服务端限制候选必须来自当前目录；置信度低于 80% 不提供可应用的图片。
- `go test -race ./...`、`go vet ./...`、JS 语法及 diff 检查通过。新增测试覆盖模型配置传递、目录外结果拒绝、低置信度、格式错误、管理鉴权与手动台标不被覆盖。
- js-reverse 连接现有 Chrome，在实际 8888 面板使用已配置模型测试“中央电视台第一套综合频道”：返回 CCTV1、置信度 100%，点击“使用此台标”填入目录内 CCTV1.webp，预览实际加载成功。仅测试新增频道表单，未保存，不修改现有频道。

## EPG 双模式与主次台标源

- 默认无数据库，以 `data/epg.json` 原子保存节目单；`system.db-type: mysql` 使用 `panel_epg_snapshots`，同一面板/API/XMLTV 输出。MySQL 连接失败不再静默退回文件模式。数据写入失败保留上次成功状态。
- 本机默认模式真实同步 168 个频道，其中 167 个返回节目，共 63,887 条；XMLTV HTTP 200，可成功解析，节目时间包含 `+0800`，M3U 自动包含 `/api/epg`。重启后仍保留 167 个频道和 63,887 条节目。
- js-reverse 登录实际面板，CCTV-1 当天显示 37 条节目及 26 个回看链接。EPG 设置、进度与存储模式正常显示。
- MySQL 分支通过存储适配接口测试，覆盖读写、失败不发布、统一 XMLTV；本机未有可访问 MySQL 实例，Docker socket 权限不足，未做 MySQL 实连测试。
- 测试覆盖节目去重、XML 转义、频道改 ID 后 EPG 关联、局部抓取失败保留旧数据、自动同步间隔和频道/EPG 认证互斥。
- 国内主源更改为 `sggc/SDU-IPTV-PRO` 的 logo 目录：Git 目录索引 2,064 张图片，规范化去重 1,917 个名称。原源为国外次源，规范化 525 个名称。地区可自动判断或在频道编辑中指定；两源 URL 可在面板配置，模型匹配共享该目录。
- GitHub API 返回 403 限流，使用实际 Git 目录生成的内置快照；更新失败保留目录。主源 CCTV1.png HTTP 200，js-reverse 确认台标加载。自定义目录缓存持久化、地区优先级和失败保留目录测试通过。
- `go test -race ./...`、`go vet ./...`、JavaScript 语法和 diff 检查通过。
